# Parsix (work-in-progress)
if cpuid -l; then # 64bit begin
  set isoname="parsix_7.0r0-amd64.iso"
  set isofile="${isopath}/parsix/${isoname}"
  set isolink="http://sourceforge.net/projects/xfardic/files/Parsix-Mirror/7.0r0/${isoname}/download"
  if [ -e ${isofile} ]; then
    menuentry "Parsix amd64" --class parsix {
      ==isocopy==
      echo "Using ${isoname}..."
      loopback loop $isofile
      linux (loop)/boot/isolinux/linux boot=parsix init=/bin/systemd vga=normal nomce quiet radeon.modeset=1 i915.modeset=1 nouveau.modeset=1 --
      initrd (loop)/boot/isolinux/initrd 
    }
  fi
fi
set isoname="parsix_7.0r0-i386.iso"
set isofile="${isopath}/parsix/${isoname}"
set isolink="http://sourceforge.net/projects/xfardic/files/Parsix-Mirror/7.0r0/${isoname}/download"
if [ -e ${isofile} ]; then
  menuentry "Parsix i386" --class parsix {
    ==isocopy==
    echo "Using ${isoname}..."
    loopback loop $isofile
    linux (loop)/boot/isolinux/linux boot=parsix init=/bin/systemd vga=normal nomce quiet radeon.modeset=1 i915.modeset=1 nouveau.modeset=1 --
    initrd (loop)/boot/isolinux/initrd 
  }
fi
